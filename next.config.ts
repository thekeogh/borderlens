import type { NextConfig } from "next";

/**
 * Next.js configuration object.
 */
const nextConfig: NextConfig = {
  allowedDevOrigins: ["dev.borderlens.tools"],
  poweredByHeader: false,
};

export default nextConfig;
